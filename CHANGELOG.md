# 更新紀錄

每天只留最新一版,當天的修改都合併在那一版. 最新在最上面.

## 2026.09.25.1(2026-09-25)

- 支援帶 0.42 吋 OLED 螢幕的 ESP32-C3 板(另一種韌體,檔名有 -oled):螢幕顯示目前油門,動力階段(緩啟動,起飛油門,第一段,往第二段爬,第二段,降落減力,等觸地),等安全開關(含自動取消倒數),等放穩,起飛倒數,結束與取消原因,拒絕啟動原因,WiFi 模式與 IP.
- 地面時螢幕輪播狀態頁(4 秒)與 WiFi 頁(6 秒),IP 用大字分兩行.
- 兩種板子(普通版 / 帶螢幕板)各有專屬韌體:板子「檢查更新」自動抓對應的檔案;手動上傳另一種板子的檔案會被擋下,目前韌體不變.
- 兩種板子接線相同,安全開關都接 GPIO21(帶螢幕板上標 TX).
- 網頁監看頁與拒絕啟動說明顯示安全開關的實際腳位;韌體卡顯示板子種類.
- 板子自己下載更新時也檢查韌體身分標記.

## 2026.09.24.2(2026-09-24)

- 降落減力新增「忽高忽低」:減力秒數內油門在高/低之間規律切換(出廠 70% / 40%,週期 1 秒,可調),提醒飛手動力快停了,走完換降落油門再判斷觸地. 每組計時器各自選,原本設定維持逐漸減力.
- 降落蜂鳴器提醒(可選):忽高忽低時跟著高油門響,逐漸減力時響半秒停半秒.
- 強制停機(馬達運轉中):長按安全開關 2 秒關馬達(馬達啟動後開關要先放開過,短路跳線不會誤關).
- 強制停機(馬達運轉中):抓著機尾左右搖擺 3 個來回關馬達,角度可調(設定頁「強制停機」,出廠 30°,0 = 關閉). 飛行中機尾只往同一邊轉,不會誤觸.
- 事件紀錄與狀態列顯示強制停機原因;設定備份碼支援新參數,舊碼照常可用.
- 系統頁家用 WiFi 名稱旁新增「搜尋」:列出附近的 WiFi(同名只列訊號最強的一筆,依訊號強弱排序,標示有無密碼),點選即填入名稱;換了不同網路會清空密碼並跳到密碼欄. 熱點模式也能搜尋.

## 2026.09.16.1(2026-09-16)

- 網頁所有 ⓘ 說明重寫得精簡扼要:44 條參數說明,設定/安裝/電變卡片說明,啟動方式等下拉選單說明,備份頁說明. 數值,警語與操作步驟都保留,只去掉重複與贅字.
- 配色改成灰藍銀:主色鋼藍銀,底色冷灰藍,降落狀態列與時間軸,紀錄圖表裡的咖啡色換掉. 警告黃,紅與飛行綠不變.
- 設定頁參數「倒數秒數」改名為「起飛倒數秒數」.
- WiFi 設定:刪掉多餘的 SSID 說明;發射功率的警告移到拉桿正下方,超過 7 dBm 時變成紅框紅字,並加上「連不上時電池接上 5 秒內拔掉,連續 3 次回出廠」.
- 備份頁:備份碼框字級加大到 16px,點下去手機不再自動放大畫面;套用框不會叫出鍵盤,長按一樣可以貼上.

## 2026.09.15.4(2026-09-15)

- 新增:**無源蜂鳴器**:安裝頁可選有源高電位觸發、有源低電位觸發或無源蜂鳴器. GPIO7 輸出 2 kHz、50% 占空比方波,沿用開機滴滴、等安全開關與倒數提示音. 舊設定與備份碼相容,切換後立即生效,按儲存保留設定.
- **安全修正(第二次安全審查)**:飛行中收到的啟動請求不會在緊急停止後被受理(原本會自動再進起飛程序);馬達停止後 2 秒內不接受啟動手勢;撞擊斷電後推飛機不啟動的鎖改成獨立記錄,按網頁「開始起飛程序」成功或重新上電才解除;韌體開始寫入時若已在等待放穩或倒數,取消回待機;控制迴圈加看門狗,萬一卡住 5 秒自動重開板子(重開期間電變失訊停機);電變輸出腳位掛載失敗時拒絕起飛並記事件.
- 安全:「油門 0% 脈寬」要先在電變頁勾「螺旋槳已拆除」才能改,並加警語(待機時馬達會立刻照新脈寬轉).
- 新增:**起飛油門**(計時器頁「起飛」區):緩啟動加到起飛油門,維持設定秒數,再用 1 秒平順換到第一段油門. 持續時間 0 = 不使用(出廠). 緩啟動 + 持續時間 + 1 秒要在第一段時間內;起飛油門超出油門上下限時時間軸文字提示實際值.
- 新增:**熱點密碼可以改**(系統頁,出廠 12345678,8~63 個英數與半形符號). 改錯或忘記:重開後 3 分鐘內沒按「保持」自動退回舊密碼;連續開關電 3 次回 12345678.
- 新增:**「備份」獨立分頁**,產生備份碼時可以勾選要包含的內容:六組風格各自勾選,起飛降落與安全(設定頁),安裝(安裝頁),電變(電變頁),WiFi(系統頁,含密碼,不要傳給別人)各自勾選,每項有 ⓘ 說明包含什麼. 預設只勾「測試」組(碼約 60 字);全部含 WiFi 約 380 字. 套用時碼裡沒有的部分完全不動;貼上後會列出碼裡有的項目,點一下可以取消不要的(變灰打 ✕);只有六組風格全部套用才會一併切換「飛行使用哪一組」. 舊格式的備份碼照樣能用,碼裡沒有的欄位保持板子目前的值(不再洗回出廠值). 含 WiFi 的碼套用當下就存起來,重新開機生效,重開後 3 分鐘內要按「保持」.
- 改變:第 6 組「測試」的名稱固定,不能改名.
- 改變:系統頁 WiFi 設定分成「自身熱點」與「家用 WiFi」兩個框,家用 WiFi 標明是 SSID 並說明只支援 2.4GHz,不支援隱藏名稱.
- 改變:分頁列改成兩行(一行 5 個),不用橫捲;分頁按鈕加亮底色與邊框,選中的分頁改實心琥珀色底,一眼看得出在哪一頁.
- 改變:**網頁改成深色航電面板風格**(藍色對眼睛不好):石墨黑底搭琥珀橙主色,淡工程格線,方角面板,數字改等寬字;綠色正常,紅色危險不變. 圖表線條顏色跟著改(監看頁第一段橘/第二段綠,紀錄頁角度紅/抖動棕).
- 改善:計時器頁時間軸圖每段不同底色並加顏色說明;不補償改斜線標示秒數;分段名稱移到圖內兩排交錯,X 軸文字不再重疊.
- 改善:角度補償頁基本油門標籤移到圖框外上方;「目前」文字跟著紅線與曲線交點移動;「機頭朝上」移到角度刻度上方;第一段/第二段按鈕加大加粗;拿掉「編輯「…」」贅字.
- 改善:頁尾 SuperGG 七彩霓虹動畫,字母由左到右依序跳動.
- 修正:**監看頁「感測器與輸出」的框會忽大忽小**:角速度與陀螺儀零點數值變動時會換行把框撐高. 這兩項改成各佔一整排,頭/翼/背三欄固定寬度單行顯示,數字緊接在自己的標籤後,欄間有分隔線;其他小格改成手機兩欄,電腦三欄的等高小格.
- 修正:**角度補償曲線拖曳放開時,點先跳回原位再跳到新位置**. 放開時角度與補償改成一次送出,送出中不再用讀回的舊值重畫;被拒絕時照樣回到原值並顯示原因.
- 改善:曲線上可拖的點縮小(手指觸控範圍不變,一樣好按);補速,減速起點上的點改成實心小點(不能拖),和可拖的空心點區分;「朝上點數」「朝下點數」各自加框.

## 2026.09.14.17(2026-09-14)

- **必要硬體:起飛安全開關**. 微動開關一端接 GPIO21,一端接地(建議串 1kΩ 電阻). 起飛程序(推飛機,網頁「開始起飛程序」,上電後直接倒數)照常開始,**按下安全開關才開始倒數**;起飛程序中按過一次就記住,倒數與飛行中放開不影響. 沒接開關或線斷掉就不會倒數,馬達不轉. 可以在設定頁停用安全開關(不建議,見下一條). 等開關時狀態燈慢閃,網頁「監看」頁顯示開關狀態.
- 新增:**設定備份碼**(設定頁最下面). 把全部設定(共用設定,六組風格與名稱,飛行使用哪一組;不含 WiFi)變成一串 LP 開頭的文字,複製起來存在 LINE 或記事本,到另一台飛機貼上,按「套用」再按儲存,設定就全部過去. 貼上時馬上檢查,少複製一段或有錯字會提示並清空. 有未儲存的變更時不能使用. 套用後請檢查感測器方位,角度修正,電變脈寬與收輪行程. 舊的備份碼在之後的韌體也能用,新增的功能維持出廠值.
- 新增:**蜂鳴器**(選配,有源蜂鳴器接 GPIO7). 開機就緒響「滴滴」;等安全開關按下時每 2.5 秒一聲長音;倒數時短音越來越急,最後 3 秒連續響到馬達啟動. 高電位響或低電位響在「安裝」頁切換(出廠高電位響). 更新後原本的設定照舊保留.
- 新增:**可以停用安全開關**(設定頁「啟動與倒數」). 停用後不按安全開關也會倒數:「上電後直接倒數」接上電池,飛機放平就倒數;「推一下才倒數」放穩就倒數. 停用前要看完風險說明並勾選三項確認;停用時網頁每一頁上方一直顯示紅色警告,事件紀錄也會記下這趟沒有經過安全開關. 出廠是使用中,停用與恢復都要按儲存才生效.
- 新增:**手動上傳韌體檔會檢查是不是這個控制器的韌體**. 選好檔案就顯示檔案版本比目前新,相同或舊(可以退回舊版);拿錯成別的 ESP32-C3 程式或其他檔案時不能上傳,目前韌體不變. 2026.09.14.16 和更早的版本沒有身分標記,不能用上傳的方式安裝.
- 改變:**角度補償曲線的點可以拉到總油門 10%~100%**,不再受基本油門限制(補償值範圍 ±50% → ±100%;例如基本油門 70% 時原本最低只能拉到 20%). 飛行中實際油門仍在油門下限與上限之間.
- 修正:**降落保險時間改從減力秒數走完後才開始算**. 降落最長 = 減力秒數 + 保險時間(例如減力 20 秒,保險 20 秒,開始降落 40 秒後才因保險時間關馬達);原本從開始降落就算,減力一走完就關馬達,沒有等觸地的時間.
- 改變:**出廠值更新**(全新板子,沒儲存過的設定,或按「回預設」時使用;已經儲存的設定不變):PWM 頻率 100Hz,啟動手勢力道 1.0g,外力門檻 0.05g,**撞擊斷電出廠關閉**(門檻 15g),單圈秒數 4.0,抖動門檻 1.0g 持續 0.7 秒,扭轉取消 15 度,轉速回傳出廠開啟(只在 DShot300 作用),起飛後 15 秒收輪,舵機速度 0.7 秒;A 組與第 6 組「測試」的飛法與曲線;出廠飛行使用第 6 組「測試」(總飛行 90 秒).
- 新增:**安全開關等待上限**(設定頁,出廠 2 分鐘,1~30 分鐘). 起飛程序開始後超過上限沒按安全開關就自動取消,回到待機;上電後直接倒數模式從電變解鎖完開始算,逾時後這次通電不再自動倒數. 等待中狀態列顯示剩餘時間,事件紀錄記原因.
- 改變:感測器腳位改成 GPIO5(SDA),GPIO6(SCL).
- 新增:網頁「系統 → 韌體更新」可檢查更新,板子自己下載安裝(需要家用 WiFi 能上網);只有網站上的版本比板子新才會提示與安裝;下載的檔案大小或檢查碼不對就放棄,目前韌體不變.
- 新增:更新中與新韌體確認前不能起飛. 板子重開後,開著的網頁會自動確認(不用按按鈕);1 分鐘內沒有網頁連上,自動退回舊版. 更新時請保持網頁開著. 由網頁確認,才能保證新韌體更新完之後你還控制得到板子.
- 新增:撞擊斷電後推飛機不會啟動,要再飛請按網頁「開始起飛程序」或拔電再接電.
- 改變:只有拔電再接電才會「上電後直接倒數」;更新韌體,網頁重開機,當機重開都不會自己倒數.
- 改變:撞擊斷電要連續 10 毫秒超過門檻才觸發,單筆感測器錯誤資料不會在空中關馬達.
- 改變:電變校正「最高油門保持秒數」出廠 3 秒:電變通電後自己要約 1 秒開機,太短電變還沒記住最高點,太長有些電變會進入設定模式. 已經調過的值不變.
- 改變:電變校正按下「下次通電進入校正」後,10 秒內沒拔電再接電會自動取消;網頁重開機也會取消. 網頁按鈕旁有提示與倒數.
- 改變:飛行中油門下限最低 10%.
- 改變:起飛程序與飛行中不能改 WiFi 設定,發射功率,檢查更新;WiFi 試用計時暫停到落地.
- 改善:網頁版面重整(說明收進 ⓘ);參數 −/+ 按一下走一格,按住連續加速.
- 改善:開機時先解開可能卡住的感測器線路,重開機後比較不會讀不到感測器.
- 改善:無線燒錄在 WiFi 較弱時比較不會中途失敗.
- 改善:韌體檔不再包含編譯用電腦的資料夾路徑(不影響功能).
- 修正:檢查更新時,板子上的版本比網站上的新也顯示「有新版本」.
- 修正:DShot 換油門偶發沒送出.

